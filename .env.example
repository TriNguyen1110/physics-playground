# Copy to .env.local and fill in after hackathon check-in (~8:30-9am).
# Everything is mocked by default (see lib/api/*.ts) so the app runs with none of these set.

WORLDLABS_API_KEY=
TRIPO_API_KEY=
MINTGG_API_KEY=

NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=
